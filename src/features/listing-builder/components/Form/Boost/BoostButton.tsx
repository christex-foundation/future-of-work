import { type Listing } from '@/features/listings/types';

export const BoostButton = ({
  listing: _listing,
  showDate: _showDate = true,
}: {
  listing?: Listing;
  showDate?: boolean;
}) => {
  return null;
};
